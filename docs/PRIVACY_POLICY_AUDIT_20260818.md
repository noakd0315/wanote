# プライバシーポリシー 実装照合（2026-08-18）

草案 `PRIVACY_POLICY_DRAFT.md` および公開用 `site/privacy-ja.html` を、
**現在の実装**と1項目ずつ突き合わせた結果です。

草案は **2026-08-11（コミット `d403464`）** 時点のもので、その後1週間で
機能がかなり増えています。

> **ポリシーは公開時点で真でなければなりません。** 実装にあってポリシーに
> 無いものは、ストアの「データセーフティ／App のプライバシー」申告と
> 食い違う原因になります。

---

## 🔴 直さないと事実と異なる（5件）

### 1. ペットの3寸法が抜けている

追加済み: **首まわり・胴まわり・着丈**（`neck_girth_cm` / `chest_girth_cm` /
`back_length_cm`）。2026-08-18 実装。

- 現在の記述: 「名前、犬種、生年月日、性別、避妊去勢の有無、体重」
- **修正案**: 末尾に「**、体のサイズ（首まわり・胴まわり・着丈）**」を追加

### 2. トイレ記録の「量」が抜けている

追加済み: **尿量**（`urine_volume`、少ない/普通/多い）。2026-08-18 実装。

- 現在の記述: 「日時、種別、尿の色、場所、写真」
- **修正案**: 「日時、種別、尿の色、**尿の量**、場所、写真」

### 3. 給餌量相談が AI の表に載っていない

第3-1章の表は「AI相談・AIレポート・証明書の読み取り」の3行だけですが、
**給餌量相談**も AI へ送信しています。送っている内容:

> 体重、ライフステージ（子犬/成犬）、避妊去勢の有無、体型の評価、
> 活動量、算出された1日あたりの必要カロリーとフード量、
> **現在与えている量**（入力されている場合）

ペットの名前・アカウント識別子は含みません。

- **修正案**: 表に1行追加

  | 給餌量相談 | 体重・年齢区分・避妊去勢の有無・体型・活動量・算出結果 | ペットの名前、個別の記録 |

### 4. ATT（トラッキング許可）は実装されていない

第4章に「iOS では、アプリの利用状況をトラッキングする前に許可を求めます」と
ありますが、**そのコードは存在しません**。

`Info.plist` に `NSUserTrackingUsageDescription` はありますが、
許可を求める呼び出し（`requestTrackingAuthorization`）がどこにもありません。
つまり**ダイアログは出ません**。

草案自身の A2 章にも「ATT（トラッキング許可）→ 未実装」とチェックが
残ったままです。

### 対応済み（2026-08-18）

**PM のご指摘どおり、これは指示済みの作業の実装漏れでした。**
`PLAN_ads.md` には出すタイミング（「広告初期化の直前」）まで設計されており、
`Info.plist` のキーも追加済み、`DATA_SAFETY_DECLARATION.md` も
「ATT の許可を求めますか → **はい**」。**コードだけが書かれていませんでした。**

`CLOUD_PHASE_TASKS.md` の D-3 が「ATT ダイアログの**実挙動を実機で確認する**」
という書き方だったため、実装済みに見えていたのも一因です。

実装しました（`app_tracking_transparency` を追加し、広告SDKの初期化直前に
許可を要求）。**ポリシーの第4章は正しくなったので、修正不要です。**

### 5. Cloudflare の役割が「中継」より広い

事業者の表で Cloudflare を「**通信内容の中継**」としていますが、実際には
**受け取って加工しています**。

AIレポートでは、アプリが**日付ごとの体重と日次のトイレ回数**を Worker へ
送り、**Worker 側で5つの数値に集約してから** Anthropic に渡しています
（`summarizeReportStats()`）。

つまり **Anthropic には日次データは渡っていません**（第3-1章の記述は正しい）が、
**Cloudflare には渡っています**。

- **修正案**: Cloudflare の行を「通信の中継および**送信内容の集約処理**」に変更

---

## 🟡 確認が必要（2件）

### 6. 広告のチェックが未完了のまま

草案 A2 章のチェックリストで「広告の表示 → `PLAN_ads.md` は未着手。
広告が入らないまま出すなら第4章と広告識別子の記述を削除する」が
**未チェックのまま**です。

**実際には AdMob は実装済みで、広告は表示されます。**
（現在は Google のテスト用広告ユニットのため、収益は発生しません）

- **対応**: チェックを付けるだけ。第4章の広告に関する記述は**残す**のが正しい

### 7. 予防医療のリマインダー設定

追加済み: 通知タイミングの個人設定（`reminder_lead_days`）。
「投薬内容、予防プログラム」に含まれると読めますが、明示していません。

- **対応**: 必須ではありません。厳密にするなら「リマインダーの設定」を追記

---

## ✅ 変更なし・記述は正しい

- **AI相談**: 質問文と、参照記録の「表示名とタグ」のみ。記録の中身・写真は送信しない → **正しい**
- **AIレポート（Anthropic向け）**: 5つの集計値のみ → **正しい**（Worker で集約）
- **証明書OCR**: 画像を送信 → **正しい**
- **生体認証**: `local_auth` は端末内で完結、送信経路なし → **正しい**
- **位置情報・連絡先・解析SDK**: 取得なし → **正しい**（依存関係を再確認済み）
- **プッシュ通知（FCM）**: 未使用のまま → **正しい**
- **お知らせ機能**: 実装済み → **正しい**
- **リマインダー**: 端末内で完結 → **正しい**
- **認証方法**: メール／Google／Apple の3つとも実装済み → **正しい**
- **PDF出力**: 2026-08-18 に削除。**ポリシーに記載が無かった**ため修正不要

---

## B章「埋める必要がある箇所」— ほぼ埋まっていました

**私の当初の報告は誤りでした。** PM のご指摘どおり、事業者名と連絡先は
すでに記入済みです。

| 項目 | 状態 |
|---|---|
| 事業者名 | ✅ `TomoakiNamekawa` |
| 問い合わせ先 | ✅ `support@wanote.jp` |
| 最終更新日 | ✅ 公開用HTMLは `2026年8月14日` |

草案（`PRIVACY_POLICY_DRAFT.md`）の冒頭だけ `【YYYY年MM月DD日】` のまま
ですが、**公開用の `site/privacy-ja.html` / `privacy-en.html` は埋まって
います**。公開するのは HTML なので、実質的な欠落はありません。

> ⚠️ **`support@wanote.jp` が実際に受信できるか**だけご確認ください。
> ストアの審査で連絡が来る可能性があり、届かないと問題になります。

> 今回の修正（下記1〜5）を反映したら、**最終更新日を今日の日付に更新**する
> 必要があります。

---

## 公開までの最短手順

1. 上記 1〜5 を修正（**私が反映できます。ご指示ください**）
2. 事業者名・連絡先・最終更新日を PM が決定
3. `site/privacy-ja.html` / `privacy-en.html` に反映
4. 公開先を決める
   - **案A**: 既存の Cloudflare Worker に `/privacy` を追加
     （新規アカウント不要。`https://wanote-functions.wanote-app-reply.workers.dev/privacy`）
   - **案B**: Cloudflare Pages に `site/` を配置（独自ドメインを当てられる）
5. URL を Play Console / App Store Connect に登録

> **文面の最終確認と公開の判断は PM にお願いします。** 法的な文書であり、
> 草案の冒頭にも「公開前に必ずご自身で確認してください」と書いてあります。
> 私は「アプリが実際に何をしているか」をコードから確認して差分を示すところまでです。
